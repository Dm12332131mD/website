"use strict";

/**
 * Returns the cube root of a number
 * @param {number} n Number
 * @returns {number}
 */
function cbrt(n) {
	console.error("Warning: Unimplemented feature | Using Math.cbrt");
	return Math.cbrt(n);
};

// Exports
module.exports = cbrt;