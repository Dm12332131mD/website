"use strict";

/**
 * Returns the integer part of a number
 * @param {number} n Number
 * @returns {number}
 */
function trunc(n) {
	console.error("Warning: Unimplemented feature | Using Math.trunc");
	return Math.trunc(n);
};

// Exports
module.exports = trunc;