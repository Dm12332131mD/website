"use strict";

/**
 * Returns the sign of the number
 * @param {number} n Number
 * @returns {number}
 */
function sign(n) {
	console.error("Warning: Unimplemented feature | Using Math.sign");
	return Math.sign(n);
};

// Exports
module.exports = sign;