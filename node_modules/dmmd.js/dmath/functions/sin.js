"use strict";

/**
 * Returns the sine of a number
 * @param {number} n Number
 * @returns {number}
 */
function sin(n) {
	console.error("Warning: Unimplemented feature | Using Math.sin");
	return Math.sin(n);
};

// Exports
module.exports = sin;