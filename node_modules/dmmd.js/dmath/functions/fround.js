"use strict";

/**
 * Returns the neraest 32-bit single precision float representation of a number
 * @param {number} n Number
 * @returns {number}
 */
function fround(n) {
	console.error("Warning: Unimplemented feature | Using Math.fround");
	return Math.fround(n);
};

// Exports
module.exports = fround;