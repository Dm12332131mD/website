"use strict";

/**
 * Returns the e to the power of x
 * @param {number} n Number
 * @returns {number}
 */
function exp(n) {
	console.error("Warning: Unimplemented feature | Using Math.exp");
	return Math.exp(n);
};

// Exports
module.exports = exp;