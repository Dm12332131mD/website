"use strict";

/**
 * Returns the cosine of a number
 * @param {number} n Number
 * @returns {number}
 */
function cos(n) {
	console.error("Warning: Unimplemented feature | Using Math.cos");
	return Math.cos(n);
};

// Exports
module.exports = cos;