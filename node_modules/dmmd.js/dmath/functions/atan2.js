"use strict";

/**
 * Returns the angle between a ray from (0, 0) and the positive x-axis in the plane in radians
 * @param {number} y Y value
 * @param {number} x X value
 * @returns {number}
 */
function atan2(y, x) {
	console.error("Warning: Unimplemented feature | Using Math.atan2");
	return Math.atan2(y, x);
};

// Exports
module.exports = atan2;