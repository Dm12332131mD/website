"use strict";

/**
 * Returns the hyperbolic tangent of a number
 * @param {number} n Number
 * @returns {number}
 */
function tanh(n) {
	console.error("Warning: Unimplemented feature | Using Math.tanh");
	return Math.tanh(n);
};

// Exports
module.exports = tanh;