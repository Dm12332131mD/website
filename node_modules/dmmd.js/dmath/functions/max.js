"use strict";

/**
 * Returns the largest of the zero or more numbers given
 * @param {...number} n Number
 * @returns {number}
 */
function max(...n) {
	console.error("Warning: Unimplemented feature | Using Math.max");
	return Math.max(...n);
};

// Exports
module.exports = max;