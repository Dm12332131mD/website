"use strict";

/**
 * Returns the square root of a number
 * @param {number} n Number
 * @returns {number}
 */
function sqrt(n) {
	console.error("Warning: Unimplemented feature | Using Math.sqrt");
	return Math.sqrt(n);
};

// Exports
module.exports = sqrt;