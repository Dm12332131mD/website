"use strict";

/**
 * Returns the base to the exponent power
 * @param {number} b Base
 * @param {number} e Exponent
 * @returns {number}
 */
function pow(b, e) {
	console.error("Warning: Unimplemented feature | Using Math.pow");
	return Math.pow(b, e);
};

// Exports
module.exports = pow;