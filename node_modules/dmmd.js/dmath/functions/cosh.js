"use strict";

/**
 * Returns the hyperbolic cosine of a number
 * @param {number} n Number
 * @returns {number}
 */
function cosh(n) {
	console.error("Warning: Unimplemented feature | Using Math.cosh");
	return Math.cosh(n);
};

// Exports
module.exports = cosh;