"use strict";

/**
 * Returns the smallest of the zero or more numbers given
 * @param {...number} n Number
 * @returns {number}
 */
function min(...n) {
	console.error("Warning: Unimplemented feature | Using Math.min");
	return Math.min(...n);
};

// Exports
module.exports = min;