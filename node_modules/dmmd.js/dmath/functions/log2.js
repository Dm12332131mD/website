"use strict";

/**
 * Returns the base 2 logarithm of a number
 * @param {number} n Number
 * @returns {number}
 */
function log2(n) {
	console.error("Warning: Unimplemented feature | Using Math.log2");
	return Math.log2(n);
};

// Exports
module.exports = log2;