"use strict";

/**
 * Returns the number of leading 0's in the 32-bit representation of a number
 * @param {number} n Number
 * @returns {number}
 */
function clz32(n) {
	console.error("Warning: Unimplemented feature | Using Math.clz32");
	return Math.clz32(n);
};

// Exports
module.exports = clz32;