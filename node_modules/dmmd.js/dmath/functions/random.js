"use strict";

/**
 * Returns a random number from 0 to 1
 * @returns {number}
 */
function random() {
    console.error("Warning: Unimplemented feature | Using Math.random");
    return Math.random;
};

// Exports
module.exports = random;