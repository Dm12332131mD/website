"use strict";

/**
 * Creates a escape tag
 * @param  {...string} args Arguments
 * @returns {string}
 */
function tag(...args) {
    if(args.some(v => typeof v !== "string")) throw new TypeError("Argument is not a string");
    return `\x1b[${args.join(";")}m`;
};

// Exports
module.exports = tag;